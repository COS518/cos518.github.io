#!/usr/bin/expect

set USER [lindex $argv 0]
set HOST [lindex $argv 1]
set PUBDIR [lindex $argv 2]
set password [lindex $argv 3]

spawn ssh $USER@$HOST "find $PUBDIR ! -path $PUBDIR | xargs chgrp -R cos518"
expect "password"
send "$password\r"
interact
